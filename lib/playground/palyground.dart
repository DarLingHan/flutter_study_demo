import 'package:flutter/material.dart';
import './routing/playground_routing.dart';

class Playground extends StatelessWidget {
  const Playground({super.key});

  @override
  Widget build(BuildContext context) {
    return PlaygroundRouting();
  }
}
